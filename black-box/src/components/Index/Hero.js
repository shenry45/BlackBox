import React from 'react';

import SearchBar from '../Searchbar';

const Hero = () => {
  return (
    <section className="hero">
      <div>
        <SearchBar />
      </div>
    </section>
  )
}

export default Hero;